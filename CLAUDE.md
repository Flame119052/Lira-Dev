# Lira — Claude Code instructions

This file is for whatever is genuinely specific to Claude Code sessions in this repo. For everything else — issue tracker, triage labels, domain docs, workflow — read `AGENTS.md`, the shared entry point every harness (Claude Code, Codex, opencode) reads.

Nothing Claude-Code-specific exists yet. Add to this file only when something genuinely doesn't belong in `AGENTS.md` because it's about how Claude Code specifically operates, not the project itself.
